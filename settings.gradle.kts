pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "spring-bybit-data-exporter-starter"

include(
    "spring-bybit-data-exporter-starter",
    "demo-app"
)
